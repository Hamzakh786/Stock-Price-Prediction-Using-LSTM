# Stock-Price-Prediction-Using-LSTM
deep learning project that predicts stock prices using an LSTM (Long Short-Term Memory) neural network. The project includes data preprocessing, sequence generation with sliding windows, model training, and performance evaluation using MAE and RMSE.
